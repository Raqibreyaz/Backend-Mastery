## 07. Path-Module (10 questions)

1. How does Express's path handling differ from Node.js's `path` module, and when should you use each?
2. Explain the difference between `path.join()`, `path.resolve()`, and `path.normalize()` in the context of Express route definitions.
3. How do you safely construct file paths for `res.sendFile()` to prevent directory traversal attacks?
4. What are the platform-specific considerations when using path modules in Express (Windows vs. Linux path separators)?
5. How do you handle relative vs. absolute paths in Express middleware, and what are the best practices for each?
6. Explain how `__dirname` and `process.cwd()` behave differently in Express applications, and when to use each.
7. How do you dynamically generate paths based on request parameters while maintaining security and preventing injection attacks?
8. What are common pitfalls when using path modules with Express's static file serving, and how do you avoid them?
9. How do you normalize paths across different environments in Express, and what utilities help with this?
10. Explain the relationship between Express route paths and file system paths when serving static files or uploads.