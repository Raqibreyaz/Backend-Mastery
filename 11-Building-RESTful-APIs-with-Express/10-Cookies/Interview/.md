## 10. Cookies (10 questions)

1. How do cookies work in Express, and what is the difference between `req.cookies`, `req.signedCookies`, and `res.cookie()`?
2. Explain the complete cookie lifecycle in Express, from setting to reading to expiration, including security attributes.
3. What are the security considerations for cookies in Express (signed cookies, HTTPS, CSP, CSRF protection), and how do you implement them?
4. How do you configure cookie options (`expires`, `maxAge`, `httpOnly`, `secure`, `sameSite`, `signed`), and what does each do?
5. What is the difference between session cookies and persistent cookies, and when should you use each in Express applications?
6. How do you implement cookie-based authentication in Express, and what are the security considerations compared to token-based authentication?
7. Explain how to handle cookie parsing in Express middleware, and what happens if cookies are malformed or missing.
8. How do you manage cookies across multiple domains or subdomains in Express, and what are the implications for `sameSite` and `domain` attributes?
9. What are common cookie-related bugs in Express applications, and how do you debug and fix them?
10. How do you test cookie functionality in Express, including setting, reading, expiration, and security attributes?